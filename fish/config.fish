if status is-interactive
    # Commands to run in interactive sessions can go here
end

#
# Alias
#

# exa
alias ls="exa --color=always --icons --sort=name -l"
alias lst="exa --color=always --icons --sort=name -lT"

